numero = int(input("Deseja fazer a soma até que número? "))

soma = 0
cont = 0
for c in range(1, numero, 2):
    if c % 3 == 0:
        cont += 1
        soma += c
print("A soma dos {} números impares entre 1 e {} é de {}".format(cont, numero, soma))
